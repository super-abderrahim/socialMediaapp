import { Box } from "@mui/material"; // Change 'box' to 'Box'
import { styled } from "@mui/system";

const FlexBetween = styled(Box)({ // Change 'box' to 'Box'
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
});

export default FlexBetween;
